import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        name="search"
        id="search"
        placeholder="Search movies..."
      />
    </div>
  );
};

export default SearchBar;
